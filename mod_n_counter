module mod_ncount#(parameter n=10,width=4)
(input clk,rst,output reg[3:0]count);
always@(posedge clk)begin
if(rst)begin
   count<=0;
   end
  else   if (count==n-1)
    count<=0;
  else  
    count<=count+1;
  end
endmodule
